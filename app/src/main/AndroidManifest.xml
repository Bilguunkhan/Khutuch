<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bilguun.khutuch">

    <application
        android:allowBackup="true"
        android:icon="@drawable/khutuchlogo"
        android:label="ХӨТӨЧ"
        android:largeHeap="true"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Test" />
        <activity android:name=".video" />
        <activity android:name=".PublicChat" />
        <activity android:name=".SendPrivateMessage" />
        <activity android:name=".Pop" android:theme="@style/AppTheme.CustomTheme"></activity>
    </application>

</manifest>